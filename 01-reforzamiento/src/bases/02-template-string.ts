const firstName = "Mike";
const lastName = "O'Neil";

// Usa backticks (``) para usar "template strings" que permite la concatenación de variables mediante el uso de ${}
const fullName = `${firstName} ${lastName}`;

console.log(fullName);



